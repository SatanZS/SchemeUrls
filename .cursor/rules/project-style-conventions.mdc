---
description: React TypeScript Vite 项目命名与结构规范
alwaysApply: true
---

# 项目风格规范

## 命名规范
- React 组件文件名与组件名使用 `PascalCase`（大驼峰命名），例如 `UserCard.tsx`。
- 变量、函数、Hook 与非组件模块使用 `camelCase`（小驼峰命名）。
- 自定义 Hook 必须以 `use` 开头，例如 `useUserProfile`。
- 非组件类资源与样式文件名使用 `kebab-case`（短横线命名）。

## 目录结构
- 页面级组件和可复用 UI 组件统一放在 `src/` 下，并保持清晰的目录边界。
- 公共工具函数放入独立目录（如 `src/utils` 或 `src/lib`），不要与组件文件混放。
- 项目内部模块优先使用 `@/` 别名导入，避免脆弱的多层相对路径。
- 每个 `.tsx` 文件只保留一个主要组件；仅允许非常轻量且强耦合的本地辅助函数共存。

## 代码风格
- 副作用逻辑放在 `useEffect` 内，渲染过程中避免产生副作用。
- 保持 TypeScript 严格模式：除非在明确边界场景，否则避免使用 `any`；必须使用时添加简短说明注释。
- 组件应保持小而聚焦，重复逻辑提取为 Hook 或工具函数。
- `import` 保持固定分组与顺序：第三方依赖优先，内部别名导入其次，本地相对路径最后。
