---
description: API 调用与错误处理规范
alwaysApply: true
---

# API 与错误处理规范

## API 调用约定
- 统一通过 API 封装层发起请求，不在页面组件中直接散落 `fetch`/`axios` 调用。
- 请求参数与响应数据应定义明确的 TypeScript 类型，避免使用无类型对象直传。
- 为接口调用设置合理超时与必要重试策略，防止页面长时间无响应。
- 同一业务域的接口按模块聚合，避免在多个目录重复维护同一接口逻辑。

## 错误处理约定
- 所有异步请求必须处理失败分支，不允许只写成功流程。
- 区分可预期业务错误与系统异常：业务错误给出可理解提示，系统异常给出通用兜底提示并记录日志。
- 不将后端原始错误直接展示给用户，避免暴露内部实现细节。
- 统一错误对象结构（如 `code`、`message`、`details`），便于页面与日志系统消费。

## UI 反馈与日志
- 加载状态、空状态、错误状态要有明确 UI 反馈，避免界面“无响应感”。
- 用户可恢复的错误提供重试入口（按钮或交互指引）。
- 关键失败路径应记录日志（含上下文信息），但禁止记录 token、密码、手机号等敏感数据。
- 组件内只处理展示与交互，错误转换和归一化尽量放在 API/服务层完成。
