---
description: 状态管理与副作用组织规范
alwaysApply: true
---

# 状态管理规范

## 状态分层原则
- 按“就近管理”原则放置状态：仅当前组件使用的状态优先放本地。
- 需要跨组件共享时再提升状态层级，避免过早引入全局状态。
- 全局状态仅承载跨页面、跨模块、生命周期较长的共享数据。
- 临时 UI 状态（弹窗开关、hover、loading）优先本地维护，不进入全局状态。

## 状态设计约定
- 状态结构保持扁平、可预测，避免过深嵌套导致更新复杂。
- 派生数据优先通过计算得到，不重复存储可推导字段。
- 更新逻辑应保持单向数据流，避免在多个位置直接修改同一份状态。
- 异步状态建议至少区分：`idle`、`loading`、`success`、`error`。

## 副作用与数据流
- 网络请求、副作用逻辑放在 Hook 或服务层，不在 JSX 渲染阶段执行。
- `useEffect` 依赖项保持完整，避免通过忽略依赖制造隐式状态问题。
- 需要复用的状态与副作用逻辑提取为自定义 Hook，组件只保留组合与展示职责。
- 复杂交互流程拆分为小步骤状态，避免单个布尔值承载多种语义。

## 可维护性要求
- 状态命名要体现业务语义，避免 `data1`、`temp` 等无意义命名。
- 删除无用状态与过期更新逻辑，避免“幽灵状态”长期留存。
- 为关键状态流转路径补充注释或测试，确保后续改动可验证。
